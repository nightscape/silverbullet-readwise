name: readwise
requiredPermissions:
  - fetch
functions:
  getBooks:
    path: readwise.ts:getBooks
    events:
      - query:rwbook
  getHighlights:
    path: readwise.ts:getHighlights
    events:
      - query:rwhighlight
